#include <iostream>
#include <utility>
#include <vector>
#include <list>
// #include "printDS.h"
using namespace std;

// template <class T>
// void printnum(T num){
//   cout << num << endl;
// }

int main()
{
  // pair<int, int> p;
  // printPair(p);

  int n = 9;
  float f = 6.77;
  double d = 567.234;

  printnum(n);
  printnum(f);
  printnum(d);

  // pair< pair<int, int>, int> p;
  // printPairP(p);

  // vector<int> v = {1, 2, 3, 4, 5};
  // printVector(v);

  // list<int> l = {1, 2, 3, 4, 5};
  // printList(l);
}
